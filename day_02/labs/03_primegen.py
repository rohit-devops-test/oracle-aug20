def genprime(start, end):

    # should create a generator with prime numbers
    # between start and end


if __name__ == '__main__':

    y = genprime(10, 100)
    print(list(y)) # Should print all primes between 10 and 100
    print(list(y)) # Should print an empty list
